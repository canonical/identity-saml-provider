name: ci
run-name: CI for ${{ github.sha }} on ${{ github.ref_name }}

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    tags:
      - "v**"
  pull_request:
    branches:
      - "*"

permissions:
  contents: read
  packages: write

jobs:
  pre-commit:
    if: ${{ github.event_name == 'pull_request' }}
    uses: ./.github/workflows/pre-commit.yaml

  openspec-check:
    if: ${{ github.event_name == 'pull_request' }}
    uses: ./.github/workflows/openspec-check.yaml

  build:
    uses: canonical/identity-team/.github/workflows/_rock-build.yaml@b5428d226e364de1c5ad601c1ec27c333f489d1d # v1.16.2

  unit-test:
    uses: ./.github/workflows/unittest.yaml

  gh-publish:
    if: ${{ (github.ref == 'refs/heads/main') || (github.ref_type == 'tag') }}
    needs: [ build, unit-test ]
    uses: canonical/identity-team/.github/workflows/_rock-gh-publish.yaml@b5428d226e364de1c5ad601c1ec27c333f489d1d # v1.16.2
    with:
      rock: ${{ needs.build.outputs.rock }}
