// Package domain defines the core business entities, value objects,
// and typed errors for the Identity SAML Provider.
//
// All types in this package are pure Go: they depend only on the
// standard library (time, fmt, strings) — never on database drivers,
// HTTP frameworks, or third-party libraries.
package domain
